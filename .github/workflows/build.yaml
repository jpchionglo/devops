jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2.3.1
      - run: dotnet build
      
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2.3.1
      - run: dotnet test -p:CollectCoverage=true

  analyze:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2.3.1
      - run: dotnet tool install --global dotnet-sonarscanner
      - run: |
          dotnet sonarscanner begin -o:'devops' -k:'devops' -d:sonar.login='521a12419d2a45a5146beb00d5e561d3797187f7' -d:sonar.host.url='https://sonarcloud.io/'
          dotnet build
          dotnet test -p:CollectCoverage=true
          dotnet sonarscanner end -d:sonar.login='521a12419d2a45a5146beb00d5e561d3797187f7'
        env: 
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#  package:
#  deploy:

name: cibuild

on:
  push:
    branches:
      - master






#person:
#  firstName: "jeremy" #string
#  lastName: 100 #number
#  middleName: true
#  alias:
#    - "dotnet"
#    - "lead"
#    - "subalias"